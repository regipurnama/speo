<%= form_tag question_path(params[:id]), method: :get do %>
  <%= label_tag 'Select Exam' %>
  <%= select_tag :exam_id, options_for_select(@exam_options, session[:exam_id]) %><br />
  <%= submit_tag 'Submit'%>
<% end %>