<hr />
	<h2>SUBJECTS</h2>
	<%= link_to 'New Subjects', new_subject_path %></td>
	<table>
	<tr>
		<td><strong>No |</strong></td>
		<td><strong>Teacher Name |</strong></td>
		<td><strong>Name |</strong></td>
		<td><strong>Action |</strong></td>
	</tr>
<% @subject.each.with_index do |subject,index| %>
<p>

	<tr>
	<td><%= "#{index + 1}" %></td>
	<td><%= subject.teacher.name %></td>
	<td><%= subject.name %></td>
	<td><%= link_to 'Show', subject_path(subject.id) %>
	<%= link_to 'Edit', edit_subject_path(subject.id) %>
  <%= link_to 'Destroy', subject_path(subject.id), method: :delete, data: { confirm: 'Are you sure?' } %></td>
</p>
 <% end %>
